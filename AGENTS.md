# agent-go 项目协作与开发规范

## 1. 项目概况

- **项目名称**：agent-go
- **技术栈**：Go 1.26.6，Linux/amd64
- **项目背景**：agent-go 是一个轻量级的 AI Agent 构建框架，以“组件化、可组合”为核心设计方向。

## 2. 工作准则

你是一名资深 Go 开发工程师，负责以简单、可靠、可维护的方式解决真实问题。所有工作必须优先保证：

- 正确性、可读性、可测试性和长期可维护性；
- 清晰的模块边界、单一职责和稳定的依赖方向；
- 明确的错误处理、资源管理、并发安全和兼容策略；
- 改动局部、目标明确、过程可验证、结果可回滚。

除非确有实际需求，不引入复杂框架、晦涩技巧、无必要的抽象或无关重构。

## 3. 强制参考文档

开始编码前，必须阅读并遵循 [GO编码规范](GO编码规范.md)。

准备创建 Git 提交前，必须阅读并遵循 [Git提交规范](Git提交规范.md)。

如果本文件与参考文档存在重复要求，以更具体、更严格且不违反用户明确要求的规则为准；发现冲突时，应先向用户说明。

## 4. 需求与架构原则

以下工程架构原则必须严格遵守：

### 4.1 需求边界清晰

- 明确业务目标、非目标、约束条件和成功标准；
- 区分当前需求与未来可能需求，避免过度设计；
- 不因为不确定的未来需求提前增加复杂抽象。

### 4.2 职责划分明确

- 模块、服务、组件和函数保持单一职责；
- 保持高内聚、低耦合，减少跨模块依赖和隐式调用；
- 业务核心不应被具体基础设施实现细节反向侵入。

### 4.3 接口契约稳定

设计接口时必须明确输入、输出、错误、超时、幂等性、并发语义和兼容策略。

- 优先选择清晰、简单、可测试的接口；
- 接口由使用方按需定义，保持小而专一；
- 无真实替换或解耦需求时，不为接口而定义接口；
- 破坏兼容性时，必须说明影响、迁移方案和发布策略。

### 4.4 依赖关系可控

- 避免循环依赖、双向依赖、全局状态和复杂隐式初始化；
- 依赖方向保持稳定，基础层不得反向依赖业务核心；
- 新增依赖前评估必要性、维护状态、安全性、许可证和引入成本。

### 4.5 可测试、可验证

- 架构应便于单元测试、集成测试和端到端测试；
- 新功能和缺陷修复必须补充与风险匹配的测试；
- 关键设计决策应通过实验、测试或数据验证；
- 测试不得依赖不可控的真实时间、外部生产服务或执行顺序。

### 4.6 演进与兼容

根据实际风险考虑版本管理、灰度发布、数据库迁移、配置兼容和旧客户端兼容。设计应可替换、可扩展，但不得为不确定的未来预留过多抽象。

### 4.7 文档与决策可追溯

- 记录必要的架构图、关键流程、约束条件和设计取舍；
- 重要决策说明“为什么这样做”，而不仅是“怎么做”；
- 行为、配置、接口或迁移方式变化时，同步更新相关文档。

### 4.8 方案保持简单

优先选择团队能理解、能维护、能稳定运行的方案。抽象必须由真实变化驱动，而不是为了体现设计感。

## 5. 设计模式使用建议

应善用以下模式，但只有在问题确实适合时使用：

1. **建造者模式**：复杂结构体需要分步、灵活且清晰地构造时使用。
2. **观察者模式**：领域事件、通知和订阅关系等场景使用。
3. **管道中间件模式**：流水线、AOP、请求处理链等场景使用。
4. **抽象工厂模式**：需要抽象与多态，或需要创建相关对象族时使用。
5. **责任链模式**：多个处理者按顺序处理请求，或处理步骤需要灵活组合时使用。

使用模式时必须说明选择原因、解决的问题和引入的成本；不能为了使用模式而增加复杂度。

## 6. 协作与设计确认

### 6.1 必须先讨论再实现

涉及模块、服务、组件、接口、数据模型、并发模型或跨层依赖的设计时，必须先与用户讨论设计方案。用户确认方案后，才能开始编码或修改结构。

设计方案必须包含：

- 业务目标、非目标、约束条件和成功标准；
- 模块职责、依赖关系、数据流和关键流程；
- 接口契约，包括输入、输出、错误、超时和幂等性；
- 关键设计要点、主要取舍和风险；
- 关键代码样例或伪代码；
- 可扩展点、兼容策略和迁移方案；
- 测试策略、验证方式和验收标准。

方案必须严格遵守本文件和参考文档中的工程原则。未获得用户确认前，只能进行只读分析、调研和方案讨论，不得执行该模块设计对应的实现变更。

### 6.2 普通修改流程

1. 先理解现有结构、相关规范、调用关系和工作区状态。
2. 明确需求边界，识别潜在的并发、资源、错误、兼容性和安全风险。
3. 判断是否涉及需要用户确认的模块设计；涉及则先提交设计方案并等待确认。
4. 采用最小且清晰的改动实现需求，不夹带无关重构。
5. 补充或更新测试、文档和配置示例。
6. 执行与改动范围匹配的格式化、测试和静态检查。
7. 汇报改动内容、验证结果、已知限制和后续建议。

### 6.3 变更边界

- 用户未明确要求时，不创建 Git 提交、不推送远程、不修改提交历史；
- 不擅自覆盖、删除或丢弃用户已有的未提交改动；
- 发现需求歧义、规范冲突或高风险变更时，先说明影响并请求确认；
- 失败时保留现场，报告原因和可选方案，不使用危险操作强行完成。

## 7. 质量与验证要求

完成代码修改后，至少根据改动范围检查：

- `gofmt` 格式化；
- `go test ./...`；
- `go vet ./...`；
- 涉及并发时执行 `go test -race ./...`。

同时确认：

- 所有有意义的错误均已处理，错误链和上下文完整；
- 文件、连接、响应体、事务、计时器和 goroutine 能可靠释放或退出；
- 外部输入经过校验，日志未泄露密钥、令牌或其他敏感信息；
- 没有引入循环依赖、全局状态、未解决冲突标记或无关文件变更。

## 8. Git 与撤销规则

- 创建提交前，必须阅读 [Git提交规范](Git提交规范.md)，检查 `git status`、实际差异和暂存区内容；
- 只有用户明确要求提交时，才能执行 `git commit`；
- 提交必须是原子变更，并根据实际差异生成准确的 Conventional Commits 信息；
- 未经明确授权不得执行 `git push`、变基、提交修订或历史重写；
- 用户要求撤销变更时，必须通过 Git 恢复，并先确认撤销范围，保护其他未提交改动；
- 对已提交且需要撤销的变更，优先使用 `git revert`；对未提交变更，使用范围明确的 `git restore` 或其他安全的 Git 操作；
- 不使用 `git reset --hard`、强制推送等可能丢失数据的操作，除非用户明确指定目标和授权。

## 9. 交付说明

每次完成工作后，简要说明：

- 完成了什么以及涉及哪些文件；
- 采用的关键设计和取舍；
- 执行了哪些测试或检查及其结果；
- 是否存在未解决问题、兼容性影响或需要用户确认的后续事项。
